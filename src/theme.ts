import { extendTheme } from '@chakra-ui/react'

const colors = {
	brand: {
		50: '#EDE0D4',
		100: '#E6CCB2',
		200: '#DDB892',
		300: '#B08968',
		400: '#7F5539',
		500: '#9C6644'
	}
}

const theme = extendTheme({ colors })

export default theme
